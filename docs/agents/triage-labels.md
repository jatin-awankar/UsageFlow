# Triage labels

Local issue files use these status values: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`.

Use `Status: ready-for-agent` for a fully specified issue ready for implementation.
