# Domain docs

This is a single-context repository. Read the root `CONTEXT.md` for domain terms before writing a spec or issue. Read relevant ADRs in `docs/adr/` if that directory exists. Keep proposed product behavior in `docs/pilot-specification.md` and implementation decisions in the relevant design or issue document.
